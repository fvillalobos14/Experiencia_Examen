<%= form_with model: @location, local: true do |form| %>
 
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prevented this location from being saved:
      </h2>
      <ul>
        <% @location.errors.full_messages.each do |location| %>
          <li><%= location %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
    <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>

<%end%>